"use client"

import { Button } from "@nextui-org/react";

const HeroButton = () => {
  return (
    <Button className="  px-8 shadow-xl py-3 bg-[white] text-white gradient-bg  rounded-full text-lg">
    Start Learning
  </Button>
  );
};

export default HeroButton;